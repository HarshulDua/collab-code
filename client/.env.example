VITE_API_URL=http://localhost:4000

# Must be the same OAuth client ID the server verifies against (GOOGLE_CLIENT_ID).
# Unset simply disables the "Continue with Google" button.
VITE_GOOGLE_CLIENT_ID=
